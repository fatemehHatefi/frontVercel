/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['m.media-amazon.com' , 'upload.wikimedia.org'], // Add the domain here
    },
  };
  
  export default nextConfig;
  